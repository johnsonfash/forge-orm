node_modules/
dist/

# Local environment overrides — never commit credentials or local URLs.
.env
.env.local
.env.*.local

# OS / editor noise
.DS_Store
*.log
*.tsbuildinfo

# Wave 5c — generated drift/reconciliation migrations (transient in this demo repo)
migrations/
